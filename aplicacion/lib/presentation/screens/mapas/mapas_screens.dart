import 'package:flutter/material.dart';

class MapasScreens extends StatelessWidget{

  static const name = 'MapasScreens';

  const MapasScreens({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Placeholder(),
    );
  }

}